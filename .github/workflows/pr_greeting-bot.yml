name: 🤖 PR Greeting Bot 

on:
  pull_request:
    types: [opened]

permissions:
  contents: read
  pull-requests: write
  
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Post comment on PR
        uses: peter-evans/create-or-update-comment@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            Hej @${{ github.event.pull_request.user.login }}, tack för din PR! Kör du dotnet test lokalt innan review?
