name: 🧪 Exercise 2 - Deploy with Secrets

on:
  workflow_dispatch:

jobs:
  deploy:
    name: 🚀 Deploy to DEVELOPMENT
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - name: 🔑 Use Secret API Key
        env:
          MY_API_KEY: ${{ secrets.MY_API_KEY }}
        run: |
          echo "Using API key (masked): $MY_API_KEY"
